meta {
  name: Buscar Subject por ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/subjects/{{subjectId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.id: isDefined
}
