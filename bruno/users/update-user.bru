meta {
  name: Atualizar User
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/api/v1/users/{{userId}}
  body: json
  auth: inherit
}

body:json {
  {
    "nome": "Prof. Atualizado"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.nome: eq "Prof. Atualizado"
}
