info:
  name: Health check
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/health

runtime:
  scripts:
    - type: tests
      code: |-
        test("Retorna 200 quando a API e o banco estão disponíveis", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
