meta {
  name: Atualizar Subject
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/subjects/{{subjectId}}
  body: json
  auth: inherit
}

body:json {
  {
    "nome": "Matéria Atualizada"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
