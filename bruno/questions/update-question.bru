meta {
  name: Atualizar Question
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/questions/{{questionId}}
  body: json
  auth: inherit
}

body:json {
  {
    "enunciado": "Questão atualizada pelo aluno."
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
